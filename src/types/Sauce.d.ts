export type Sauce = {
    id: string, 
    nom: string, 
    quantite: string[], 
    prix: number, 
    description: string, 
    ingredients: string[], 
    caracteristique: string, 
    created_at: string, 
}